using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class UIController : MonoBehaviour
{
    public const float CatagoryOffsetY = 10.0f; //TODO figure out what the const amount should be for this
    public const float CatagoryTileExtend = 20.0f;

    [SerializeField] private GameObject DayToDayTile;
    [SerializeField] private GameObject ReoccurringTile;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
